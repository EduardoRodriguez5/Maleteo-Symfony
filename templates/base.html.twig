<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/meyer-reset/2.0/reset.min.css">
        <link href="https://fonts.googleapis.com/css?family=Catamaran:400,500&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <link rel="stylesheet" href="http://localhost:8000/assets/styles/styles.css">
        
        <title>{% block title %}Maleteo{% endblock %}</title>
        {% block stylesheets %}
        {% endblock %}
    </head>
        <div class="wrapper">
            <div class="container-fluid">
                <header class="header">
                    <a href="{{path("maleteo")}}"><img src="http://localhost:8000/assets/img/logo.svg" alt="Maleteo"></a>
                    <ul class="nav navbar-nav navbar-right">
                        {% if app.user == null %}
                        <div style=" display: flex; flex-direction: row; justify-content: flex-end;">
                            <li><a href="{{path('signUp')}}"><span style="padding: 10px"></span> Registrarse</a></li>
                            <li><a href="{{path("app_login")}}"><span style="padding: 10px"></span> Login</a></li>
                        </div>
                        {% else %}
                        <div style=" display: flex; flex-direction: row; justify-content: flex-end;">
                        <li><a href="#"><span style="padding: 10px"></span> {{app.user.username}}</a></li>   
                        <li><a href="{{path("app_logout")}}"><span style="padding: 10px"></span> Cerrar Sesión</a></li>
                        </div>
                        {% endif %} 

                      </ul>
                </header>
            </div>
        </div>
    
    <body>
        {% block body %}
        {% endblock %}
    </body>    
        {% block javascripts %}{% endblock %}
    <footer class="footer">
        <div class="wrapper">
            <div class="container-fluid">
                <img src="http://localhost:8000/assets/img/logo.svg"
                    alt="Maleteo"
                    class="footer__logo">
                <p class="footer__claim">
                    Hecho con ❤️ por Eduardo Rodríguez González.
                </p>
            </div>
        </div>
    </footer>
</html>
